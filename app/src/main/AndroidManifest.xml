<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sunofbeach.customview">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CustomView">
        <activity android:name=".view.watchfaceview.WatchFaceActivity"></activity>
        <activity android:name=".viewgroup.slidemenu.SlideMenuViewActivity" />
        <activity android:name=".viewgroup.keypad.KeyPadActivity" />
        <activity android:name=".viewgroup.flowview.FlowLayoutActivity" />
        <activity
            android:name=".loginkeyboard.LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".loginkeyboard.LoginKeyBoardActivity" />
        <activity android:name=".customview.InputNumberActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>